// Entry for "Budget at a Glance.html" (Home).
// The heavy lifting (React prelude + concatenated page modules) is produced by
// the `budget-pages` Vite plugin in vite.config.mjs.
import "virtual:budget-home";
